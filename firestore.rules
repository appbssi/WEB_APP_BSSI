rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Sécurité renforcée : authentification obligatoire pour lire et écrire dans la base de données
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}